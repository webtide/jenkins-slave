apiVersion: v1
kind: PersistentVolume
metadata:
  name: task-pv-nexus
  namespace: nexus
  labels:
    type: local
spec:
  storageClassName: microk8s-hostpath
  capacity:
    storage: 140Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /app/nexus-storage/nexus-data
