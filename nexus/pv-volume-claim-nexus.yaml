apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: task-pv-claim-nexus
  namespace: nexus
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 120Gi
